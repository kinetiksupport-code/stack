import { useState } from "react";
import { Bell, Check, SlidersHorizontal } from "lucide-react";
import { useLocation } from "wouter";
import StackMark from "@/components/StackMark";
export default function Settings() { const [, setLocation] = useLocation(); const [saved, setSaved] = useState(false); const [autoSave, setAutoSave] = useState(true); const [updates, setUpdates] = useState(true); const save = () => { setSaved(true); setTimeout(() => setSaved(false), 1800); }; return <main className="min-h-screen bg-[#ededed] p-3 font-sans text-[#161616] sm:p-4"><div className="mx-auto min-h-[calc(100vh-24px)] max-w-[1280px] overflow-hidden rounded-[24px] border border-black/[.07] bg-[#f7f5f1] shadow-[0_16px_70px_rgba(82,63,47,.12)] sm:rounded-[30px]"><header className="flex items-center justify-between border-b border-black/[.08] bg-white/80 px-5 py-4 sm:px-8"><button onClick={() => setLocation("/workspace")}><StackMark size="sm" showWordmark /></button><button onClick={() => setLocation("/workspace")} className="text-xs font-semibold text-neutral-500 hover:text-neutral-900">Back to workspace</button></header><div className="mx-auto max-w-[760px] px-5 py-12 sm:px-10 sm:py-16"><div className="mb-10"><div className="mb-4 inline-flex rounded-full bg-[#ef4d23]/10 p-3 text-[#ef4d23]"><SlidersHorizontal size={18} /></div><p className="text-[10px] font-bold uppercase tracking-[.2em] text-[#ef4d23]">Workspace</p><h1 className="mt-2 font-serif text-5xl tracking-[-.06em]" style={{ fontFamily: "'Instrument Serif', Georgia, serif" }}>Settings</h1><p className="mt-3 text-sm text-neutral-500">Shape how Stack feels while you build.</p></div><div className="divide-y divide-black/[.08] rounded-2xl border border-black/[.08] bg-white shadow-sm"><div className="p-5 sm:p-6"><h2 className="text-sm font-semibold">Editor preferences</h2><div className="mt-5 space-y-4"><label className="flex items-center justify-between gap-4 text-sm text-neutral-600"><span><b className="block text-neutral-800">Auto-save changes</b><small className="text-xs text-neutral-400">Keep your latest edits safe automatically.</small></span><button onClick={() => setAutoSave(!autoSave)} className={`h-6 w-11 rounded-full p-1 transition ${autoSave ? "bg-[#ef4d23]" : "bg-neutral-200"}`}><span className={`block size-4 rounded-full bg-white transition ${autoSave ? "translate-x-5" : "translate-x-0"}`} /></button></label><label className="flex items-center justify-between gap-4 text-sm text-neutral-600"><span><b className="block text-neutral-800">Build updates</b><small className="text-xs text-neutral-400">Show progress while Stack is generating.</small></span><button onClick={() => setUpdates(!updates)} className={`h-6 w-11 rounded-full p-1 transition ${updates ? "bg-[#ef4d23]" : "bg-neutral-200"}`}><span className={`block size-4 rounded-full bg-white transition ${updates ? "translate-x-5" : "translate-x-0"}`} /></button></label></div></div><div className="p-5 sm:p-6"><h2 className="flex items-center gap-2 text-sm font-semibold"><Bell size={15} /> Notifications</h2><p className="mt-2 text-xs leading-5 text-neutral-400">Product updates and build completion alerts will appear here.</p></div></div><button onClick={save} className="mt-6 flex items-center gap-2 rounded-full bg-[#0b0f1a] px-5 py-3 text-xs font-semibold text-white">{saved ? <Check size={14} /> : null}{saved ? "Saved" : "Save preferences"}</button></div></div></main>; }
